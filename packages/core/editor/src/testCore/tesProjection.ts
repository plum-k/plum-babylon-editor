import {ProjectionTransform, Viewer} from "@plum-render/babylon-sdk";

const tesProjection = (viewer: Viewer) => {
    const projectionTransform = new ProjectionTransform();




}

export default tesProjection